# 音效系统说明

> 落地日期：2026-08-12。对应策划音效需求清单「音效需求——11 个」（8 大类）。
> 音频源文件：`Assets/Resources/SoundEffect/`（12 个 mp3，文件名 = 需求编号_语义_日期）。

## 0. 首次启用（必做一次）

代码已全部接好，但音效表资产需要在 Unity 里生成一次：

1. 打开 Unity，等编译完成（会自动生成新脚本的 .meta）。
2. 执行菜单 **MasterHouse → 音效系统 → 创建音效表（补齐缺失）**——生成 `Assets/Resources/OutGameUI/SfxTable.asset` 并按文件名前缀自动匹配全部剪辑。
3. 进 OutGameTest 场景冒烟。缺表时 Console 会报错并指向该菜单，不会静默无声。

音效表也已注册进 **配置中心**（MasterHouse → 配置中心）的「音效」分组。

## 1. 架构：一表一管理器，播放点只写 ID

```
SfxTable.asset（Model，运行时只读，§16.6）      HouseSettings（sfxVolume 0~100，§16.5 设置文件）
   ESfx → 剪辑 / 音量倍率 / 节流间隔                        │
        └──────────────┬───────────────────────────────────┘
                       ▼
              SfxManager（View 层全局单例，DontDestroyOnLoad）
              · 唯一出口：SfxManager.Play(ESfx.Xxx)，fire-and-forget
              · 单 AudioSource + PlayOneShot（多音自然混音）
              · 同 ID 按 minInterval 节流（防同帧叠爆；打字机节奏也靠它）
              · 订阅业务事件自动发声（见 §2 第 4/6 条）
                       ▲                        ▲
        UI 交互音：各 View 直接调 Play      业务事件音：VisitorManager / EconomyManager 的
        （点击/转场/拖拽/推进）              事件广播，业务代码零音频调用（MVVM 分层不破）
```

三个代码文件（Core 模块，局内局外通用）：

| 文件 | 职责 |
|---|---|
| `Assets/Scripts/Core/SfxTable.cs` | `ESfx` 枚举（11 个 ID + None）、`SfxEntry`、`SfxTable` SO |
| `Assets/Scripts/Core/SfxManager.cs` | 全局播放器：加载表、音量、节流、订阅业务事件 |
| `Assets/Scripts/Core/Editor/SfxConfigSetupUtility.cs` | 建表菜单：只补缺失不覆盖，剪辑按前缀自动匹配 |

拉起时机：`OutGameBootstrap` 在 GameManager 之后、界面壳之前 `SfxManager.Ensure()`（进场即订阅访客/经济事件）。`Play` 内部有惰性兜底，早于拉起的调用不炸。

**约定：**
- 播放点一律 `SfxManager.Play(ESfx.Xxx)`，禁止散落 `AudioSource`/直接引用剪辑。
- 换音 / 调音量 / 调节流 = 改 `SfxTable.asset` 的 Inspector，不碰代码。
- `ESfx` 枚举值已显式赋值并序列化进资产，**新增只能追加，不得改已有值**。
- 音效属表现层：只订阅业务事件、只读状态，绝不回写业务（§16.4 表现层豁免边界）。

## 2. 需求 → 实现映射（11 个全接）

| # | 需求 | ESfx | 剪辑 | 挂接点（单点收口） |
|---|---|---|---|---|
| 1 | 基础点击 | `UiClick` | 1_Button | `OutGameTweenButton.OnPointerClick`——该组件已挂在 125/128 个局外按钮上（Prefab 静态挂 + `BindButton` 运行时兜底），零散布点全覆盖；置灰按钮不响 |
| 2 | 家具拾起 | `FurniturePickup` | 2_Pickup | `FurnitureRoomController.BeginDrag`（收纳栏拖出与场上拿起两条路径的汇合点） |
| 2 | 家具放置 | `FurniturePlace` | 2_Putdown | `FurnitureRoomController.EndDrag` 两个 commit 成功分支（落地 / 收回收纳栏）；无效落点不响 |
| 3 | 客人交互 | `GuestInteract` | 3_Interaction | ①`HubPage.SelectGuest`（访客卡与场景 NPC 两条点击路径的汇合点）②`DialogueOverlay.OnAdvanceClicked`（点击继续，含跳全文）③`DialogueOverlay.OnOptionClicked`（分支选项，键盘回车也汇到这） |
| 4 | 数值正向 | `ValueGain` | 4_ScoreGain | `EconomyManager.Feedback` 事件 → SfxManager 订阅。发声处：服务结算（`CompleteGuestService`）、对话加声望 |
| 4 | 数值负向 | `ValueLose` | 4_ScoreLose | 同上。发声处：拒绝/超时扣声望（`RefuseGuestService`）、对话扣声望/扣货币 |
| 5 | 页面转场 | `PageTransition` | 5_Transition | ①`HouseUIManager.ShowPage`（整页切换，启动首页不响）②`HubPage.SelectRoom`（切房间）③`HubPage.OpenFurnitureMode` 进入 + 退出回调 |
| 6 | 访客到来 | `VisitorArrive` | 6_InfoCome | `VisitorManager.InstanceSpawned` 事件 → SfxManager 订阅（GM 召唤同样触发） |
| 6 | 访客离开 | `VisitorLeave` | 6_InfoLeave | `VisitorManager.InstanceDeparted` 事件 → SfxManager 订阅 |
| 7 | 资源&奖励 | `Reward` | 7_Reward | ①商城购买成功（`StoreOverlay.BuySelected` / 家具模式 `OnPurchaseConfirmed`）②对话奖励发货币（经济 `CurrencyGain` 反馈）——物资入手走同笔对话事件 |
| 8 | 对话逐字 | `DialogueTyping` | 8_TypingKeyboard | `DialogueTypewriter.Update`：每有新字符上屏请求一次，节奏由表里 `minInterval`（0.055s）节流；跳全文不补发 |

备用剪辑：`8_TypingPhone` 未入表，想换手机打字音 = 在 SfxTable 的 DialogueTyping 条目上换剪辑引用。

## 3. 刻意不响的场合（防叠音/防噪音的设计决定）

| 场合 | 原因 |
|---|---|
| 访客卡、对话推进/选项按钮的基础点击音 | `BindButton(..., ESfx.None)` 关掉——这些按钮响的是更具体的交互音（#3），不叠基础点击 |
| 商城购买的扣款负向音 | 购买的反馈就是那声 `Reward`（获得弹窗），扣款再响负向音语义打架 |
| GM 后门（`GmAdd*`） | 调试操作，`Feedback` 事件刻意不发；GM 面板按钮本身的点击音照常 |
| 装饰分回写（`SetFurnitureDecorationScore`） | 每次摆放家具都会重算，若发声会与摆放音（#2）每次都叠响 |
| 全屏遮罩（scrim）点击关面板 | 3 处无手感组件的遮罩按钮保持静音——遮罩不是「普通按钮」 |
| 无效操作 | 分支未选点空白、家具无效落点、点已离开的访客等：不响，静默即反馈 |

## 4. 常见改动怎么做

- **换某个音效**：选中 `Resources/OutGameUI/SfxTable.asset`，在对应条目换 `clip`。
- **调某个音效音量**：同上调 `volume`（0~2 倍率，乘在全局音量上）。
- **打字机节奏**：调 DialogueTyping 条目的 `minInterval`（秒/次）。
- **加一个新音效**：①`ESfx` 追加新枚举项（显式赋值，勿动已有值）②表里加条目配剪辑 ③播放点写一行 `SfxManager.Play(ESfx.新ID)`；若想被建表菜单自动匹配，给 `SfxConfigSetupUtility.Mapping` 加一行前缀映射。
- **全局音量**：`HouseSettings.Data.sfxVolume`（0~100），存 `persistentDataPath/house-settings.json`，每次播放实时读取——改完立刻生效，无需重启。

## 5. 已知边界与后续待办

- **设置页还没有音量滑条**：`sfxVolume` 字段与消费链路已通（默认 78），但设置 Prefab 里没有 Slider 控件。补滑条要动手调 Prefab（Prefab 是布局唯一真相源，§16.2），留待下次 UI 手调时一并做：`OutGameSettingsPageView` 加 Slider 字段 → `TitleSettingsPage.BindContent` 绑定 → 写回 `HouseSettings` + `Save()`。
- **BGM 未接**：目前只有 SFX 素材；`bgmVolume` 字段同样已预留。BGM 进场时建议在 SfxManager 旁加独立循环 AudioSource，不复用 PlayOneShot 通道。
- **局内（NodeSim）无 UI 音**：局内测试场景是 IMGUI 调试面板 + 世界空间交互，无 uGUI 按钮；将来要接，挂点在 `InteractionController`（选中/放置/删除）与产线事件，`SfxManager.Play` 在局内场景同样可用（惰性自建）。
- **「局内局外变更」转场音**：两场景当前物理隔离、无切换路径（待定 #19），联通时在切换入口补一行 `Play(PageTransition)`。
- **「资源解锁」时刻无独立音**：声望解禁（`IsFurnitureRevealed`）是纯查询无事件，解禁瞬间无人知晓；如需要，得给 EconomyManager 加声望阈值跨越检测，暂缓。
