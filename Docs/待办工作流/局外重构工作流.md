# 局外重构工作流（进度跟踪）

> 创建：2026-08-08。本文件是局外重构的**执行清单与进度记录**，跨会话续作时先读本文件再动手。
> 决策依据（权威，本文只引用不复述）：`Docs/架构设计.md` §15【全工程模块化目录规则】、§16【局外系统架构】；现状参考：`Docs/局外系统说明.md`。
> 用法约定：任务完成勾选 `[x]`；开始一项时可标注 `（进行中 日期）`；每次工作结束在文末【进度日志】追加一行（日期 + 完成内容 + 遗留问题）。任务本身有疑义时回到架构文档对应小节，不在本文件里改决策。

---

## 阶段一 · 点亮（先行，目标：近几天内完成）

> 目的：只修合并遗留、不动架构，让旧代码全功能可运行，作为后续重写的**功能基线**（§16.1）。

- [x] 1.1 新建局外专用测试场景 `Assets/Scenes/OutGameTest.unity`，场景内「局外入口」物体挂 `OutGameBootstrap`，显式调用 `OutGameUI.Build()` 并默认拉起 `HouseGmConsole`（不恢复 `RuntimeInitializeOnLoadMethod` 自动注入；与局内 TestScene 互不加载，§16.10）。Bootstrap 按用户拍板放 `Assets/Scripts/Core/`，承接局外初始化
- [x] 1.2 补建缺失的 `HouseEconomyConfig.asset`（实际未走菜单工具：按代码默认值手写资产、用户确认结构后落地，与菜单产物逐字段一致）
- [x] 1.3 全功能冒烟，对照 `局外系统说明.md` §2【功能总览】逐项过：标题页 → 存档页 → 开门过场 → Hub → 访客服务/拒绝 → 周结算 → 11 类面板 → 家具摆放与烘焙 → 观景模式 → 退出写档
- [x] 1.4 冒烟发现的问题记入【进度日志】；只修阻断性 bug，不做任何架构性修改（本轮未发现阻断问题）

**验收标准**：旧代码全功能可运行、可演示。

## 阶段二 · 重写前准备

- [x] 2.1 目录重组（§15.2）：抽取 `GameManager`/`GameConfig` 入 `Core/`；局内现有 Data/ViewModel/Manager/View/Interaction/Debug/Editor 平铺目录整体平移进局内模块目录（暂定名 `NodeSim/`）。**纯机械平移，必须单独提交，不夹带逻辑改动**（实际拆分细节：`FurnitureConfigSetupUtility.cs` 属局外，从 Editor/ 改移 `Furniture/Editor/`；顺带清理空目录 `Tool/`）
- [ ] 2.2 （可选清理）删除已合并进主线的远程分支（`codex/outgame-prefab-ui` 等）

## 阶段三 · 逐模块重写

> 总则（§16.1/§16.2）：旧代码冻结只读；**新模块验收一个、删除对应旧代码一处**。验收 = 对照阶段一基线行为等价，明示豁免两处：存档功能移除（§16.5）、仓库/个人/通讯录占位页（§16.8）。
> 硬约束：局外逻辑层遵守 §11 全部守则；Prefab 是布局唯一真相源，禁止 Bind/Build 双实现（§16.2）；内容数据只进 Def 资产（§16.6）。

- [x] 3.1 **HouseClock**：tick 驱动的时钟与时段划分（§16.4）；倍率进 GameConfig 可配字段（待定 #18 注释占位）
- [x] 3.2 **Economy**：`HouseEconomy` 平移为 EconomyData + EconomyManager；切断对 `OutGameUIData` 的反向依赖（§16.7 毒点①）；`HouseGmConsole` 跟随本模块
- [x] 3.3 **内容 Def 化**（§16.6）：VisitorDef / RoomDef / DeviceDef / Codex 内容（档案/世界观/成就/日记）/ 台词与面板文案；消灭管道分隔字符串（范围豁免：委托面板假任务与嵌入排版模板的零散文案留待 3.5/委托系统设计；时段文案进 HouseClock 静态映射不进 SO）
- [x] 3.4 **Visitor**：VisitorManager 业务重写（进场判定/服务窗口/服务与拒绝结算/周结算，全部挂 tick、整数比较）；表现层四脚本平移接新数据，表现结果不回写业务（§16.4）
- [ ] 3.5 **HouseUI 壳**：Canvas 生命周期、页面路由、ESC 退栈；逐页迁移（标题 → 存档页占位 → 过场 → Hub → 面板栈），每页绑定逻辑独立成文件；砍掉代码兜底路线（进行中 2026-08-09：3.5a 壳+标题簇、3.5b 过场+Hub 主体完成；迁移方式=新旧并行 + Bootstrap 开关；余 3.5c 面板栈+对话、3.5d 对照收尾）
- [ ] 3.6 **面板补齐**（§16.8）：商城补 Prefab；Hub 设置复用标题设置 Prefab；仓库/个人/通讯录统一占位页
- [ ] 3.7 **Dialogue 接缝**（§16.9）：台词 Def + 独立播放接口，现有访客事务面板走接缝取内容
- [ ] 3.8 **Furniture 断毒点**（§16.7）：家具配置表并入 Def 体系；家具场景层不再依赖局外 UI 工厂（毒点②）；内部 MVVM 化**不做**（二轮）
- [ ] 3.9 **旧代码清零**：`OutGameUI` / `OutGameUIData` / `OutGameClock` / `OutGameUIFactory` 界面搭建职责全部退役删除；Prefab 生成器归入 HouseUI 的 Editor 子目录并补命名空间
- [ ] 3.10 **文档收尾**：`局外系统说明.md` 改写或归档；架构设计 §12/§16 状态更新；CLAUDE.md 校对

## 等待外部输入（阻塞项，不排期）

| 事项 | 阻塞什么 | 对应待定项 |
|---|---|---|
| 对话系统功能要求（用户/策划给出） | 3.7 接缝之后的完整对话系统实现 | 待定 #17 |
| 统一存档数据结构设计访谈 | 存档功能重新接入（局内局外同一份 JSON） | 待定 #9 |
| 局外时间倍率定案与离线结算 | GameConfig 字段定值、读档补算 | 待定 #18 |
| 局内外联通设计 | 两侧场景/相机整合 | 待定 #19 |

## 进度日志

- **2026-08-08**：/grill-me 访谈完成，全部决策落地为架构设计 v0.4（新增 §15/§16，待定 #9 定方向，新增 #17~#19）；CLAUDE.md 与局外系统说明.md 同步更新；本工作流创建。阶段一尚未开始。
- **2026-08-08**：完成 1.1——新建 `Assets/Scripts/Core/OutGameBootstrap.cs`（用户拍板放 Core 而非 UI，作为局外初始化承接点；.meta 由编辑器生成后回填场景引用）与 `Assets/Scenes/OutGameTest.unity`（Main Camera + 局外入口）。待用户在编辑器执行 1.2 配置表菜单并按 1.3 清单冒烟。
- **2026-08-08**：完成 1.2~1.4——用户冒烟通过、无阻断问题（当时经济数值走的是内置默认值兜底）；随后核实配置表菜单未执行，`HouseEconomyConfig.asset` 按代码默认值手写补齐。**阶段一验收达成，旧代码基线可用**。
- **2026-08-08**：完成 2.1——全部 `git mv` 平移（.meta 跟随、GUID 不变、零代码改动）：GameManager/GameConfig → `Core/`；Data/ViewModel/Manager/View/Interaction/Debug/Editor → `NodeSim/`；家具配置工具 → `Furniture/Editor/`；删除空目录 `Tool/`。待编辑器重新聚焦后确认编译与两场景运行正常；新目录 `NodeSim/`、`Furniture/Editor/` 及 `HouseEconomyConfig.asset` 的 .meta 由编辑器生成后补一笔提交。
- **2026-08-09**：完成 3.1——新建 `HouseClock/`：`HouseClockData`（唯一时间状态 Day + TickOfDay，分钟/时段全部整数派生，时段枚举 `EHousePhase` 对齐旧 0~5 下标）+ `HouseClockManager`（Tick/NextDay/ResetNew + IsRunning 闸门，保留「时间只在 Hub 流动」基线行为）。接入：GameConfig 增 `HouseTicksPerGameMinute`（待定 #18，默认 10 = 原型节奏）；GameManager 抽 `RunTick()` 统一心跳驱动局内外（含 StepOneTick）；OutGameBootstrap 在无 GameManager 时代码创建（场景保持只含 Bootstrap，§16.10）。旧调用点（OutGameUI 约 12 处 / OutGameUIData.CurrentPhase / OutGameVisitorStage 3 处）改走标注「过渡桥接」的私有访问器，业务逻辑零改动；`OutGameClock.cs` 删除。过渡兼容两处 float 派生属性：`HourF`（3.4 访客重写改整数比较后删）、`MinuteOfDayF`（旧存档 v3 口径，待定 #9 后删）。**遗留**：待编辑器聚焦确认编译，按基线冒烟（时钟走字/时段与 DAY 刷新/访客到点进场/服务窗口提示/周结算/存读档/GM 重置）；`HouseClock/` 目录 .meta 待编辑器生成后补提交；2.2 已核实三条远程分支均并入 master、等用户拍板是否删除。
- **2026-08-09**：2.2 用户拍板不删远程分支，任务关闭（保持未勾选、不再排期）。完成 3.2——新建 `Economy/`：`EconomyData`（货币/声望/GM 加成/家具装饰分/房间与设备数量/所有权集合 + `FurniturePurchaseResult` + `EconomySaveData`）+ `EconomyManager`（旧 `HouseEconomy` 全部逻辑逐条平移：服务/拒绝/周结算、购买双门、Capture/Restore 旧档字段兼容、GM 三接口、Changed 事件；装饰分为 Manager 派生值）。**毒点①已断**：Manager 不再读 `OutGameUIData`，房间/设备数量经 `SetDecorSourceCounts` 由旧 OutGameUI.Awake 推入（统计方法挪到 OutGameUIData，3.3 Def 化后换推入源）。`HouseEconomyConfig.cs` → `Economy/EconomyConfig.cs` 改类名（GUID 不变，资产文件名与 Resources 路径按用户拍板保持不动）；`HouseGmConsole` git mv 入 `Economy/`；GameManager.Awake 创建 EconomyManager（纯事件驱动不进 RunTick）。调用点迁移：OutGameUI/FurnitureRoomController/GmConsole 全部改走过渡桥接访问器，退订处加 GameManager 判空守卫；`HouseEconomy.cs` 删除。**遗留**：待编辑器确认编译，按基线冒烟（初始 2480/40、服务 +320/+25、拒绝 -15、周结算 -30×n、商城与家具模式购买、「？」解禁、装饰分随摆放变化、GM 三键与全量重置、旧存档经济段读回）；`Economy/` 目录 .meta 待编辑器生成后补提交。
- **2026-08-09**：3.2 冒烟通过，`Economy/` .meta 已补提交。3.3 开工（第一步）——方案定稿：两个表式 SO（跟随 FurnitureTable 模式），`Visitor/VisitorDef.cs`（VisitorDef 收编 Guests 全字段 + 序列帧路径 + 事务台词；AmbientVisitorDef 消灭管道字符串）与 `Codex/CodexDef.cs`（RoomDef/DeviceDef（level 转 int、owned 转 bool、roomId 约定式分组）/CodexEntryDef（档案两 tab 合一类 + category 枚举）/AchievementDef/JournalEntryDef + 查询与统计方法）。用户拍板：模块名维持 Codex；**委托后续可能独立为单独一类内容，等需求确定**（本轮委托面板假任务不建 Def，P2 不预建）。时段文案（PhaseNames/Ranges）将随消费切换挪进 HouseClock 静态映射（与划分边界同源，不进 SO）。迁移工具 `UI/OutGameContentMigrationTool.cs`（运行时目录 + UNITY_EDITOR 包裹，因需读 internal 旧数据；成就/日记旧代码无单一来源、工具内誊抄为唯一来源；DialogueLine 临时 internal）。**待用户**：编辑器执行菜单 MasterHouse → 局外内容 → 生成内容表，Inspector 过目两资产（访客 4+邻居 6/房间 4+设备 9+档案 9+成就 4+日记 2），然后我切消费代码并删旧硬编码。
- **2026-08-09**：完成 3.3（第二步）——用户已生成并过目两资产（已提交）。消费切换：GameManager 加载并暴露 VisitorTable/CodexTable（缺失打 Error）；OutGameUI 全部面板绑定（Hub 卡片/导航/对话/委托/设备/日记/通讯录/档案/日历）与 OutGameVisitorStage 改读内容表（过渡桥接访问器 Visitors/Codex + 档案分类缓存）；设备面板从管道字符串解析改 DeviceDef（LV 文案由 int 格式化）；日记/成就面板改遍历 Codex 列表（成就完成态保持「前两项 ✓」假状态）；台词改读 VisitorDef.transactionLine。**毒点①最终形态**：EconomyManager 构造时由 CodexTable 统计房间/设备数量，SetDecorSourceCounts 推入桥接与 OutGameUIData.CountOwnedDevices 删除。时段文案挪入 HouseClock 的 HousePhaseText 静态映射。删除：OutGameUIData 全部内容数组与 OutGameRoom/OutGameGuest/OutGameArchiveItem 三类、DialogueLine、迁移工具（OutGameUIData 只剩存档结构 + CurrentPhase 桥接）。**遗留**：待编辑器确认编译，冒烟重点=Hub 访客卡与舞台进场、四房间切换与背景、11 面板内容显示（设备 LV 文案、档案两 tab、日记/成就、通讯录、对话台词）、周结算、存读档；装饰分应仍为房间 4×50+设备 6×30 口径。
- **2026-08-09**：3.3 冒烟通过。完成 3.4——新建 `Visitor/VisitorData.cs`（VisitorState：Arrived/Served/Refused）+ `VisitorManager.cs`（构造注入 VisitorTable/HouseClockManager/EconomyManager；Tick 内整数分钟进场判定、CanServe 整数窗口、Serve/Refuse/EndWeek 结算含经济与时钟联动、旧存档三数组 Capture/Restore 过渡壳；时钟闸门关闭时业务不推进；事件广播暂缺=旧 UI 自行刷新，3.5 需要时再加）。GameManager RunTick 顺序=时钟先走访客后判。表现层四脚本 git mv 入 `Visitor/`；Stage 砍 served/arrived 参数与 **onGuestArrived 回写回调（§16.4 反例废除）**，改每帧轮询业务状态；OutGameUI 三个业务数组删除、约 25 处改走 Visitor 桥接。过渡债兑现：删 `HouseClockData.HourF` 与 `VisitorDef.InServiceWindow(float)`。**已知表现级差异**（方向正确，记录备查）：①玩家不在起居室时到访照常推进，回房后访客直接在屋内（旧版补走门动画）；②舞台重建（切房间回来）一律屋内淡入，实时到访才走门。**遗留**：待编辑器确认编译，冒烟=到点走门进场、窗口外点击提示、完成/拒绝结算与演员反应、周结算跳次日、存读档（三数组格式不变）、GM 重置；`Visitor/` 新脚本 .meta 待编辑器生成后补提交。
- **2026-08-09**：3.4 冒烟通过、.meta 已补。3.5 方案定稿（用户拍板三项：新旧并行+开关迁移；设置独立 JSON 现在做；动态列表模板走生成器扩展）+ **3.5a 完成**——新建 `HouseUI/`：`HouseUIManager`（Canvas 参数与旧壳一致、整页路由、叠加层栈+ESC「先弹栈再问页」、Toast）、`HousePage` 页面基类（Prefab 缺失打 Error 不回退，§16.2）、`IHouseOverlay`（3.5c 用，先立 ESC 语义）、`PaperPage<T>` 纸张页基类（返回/字体/入场动效统一）、`HouseUIUtil`（字体/颜色/字距/Tween 清理，不依赖 OutGameUIFactory）、`HouseSettings`（§16.5 全局设置 JSON：persistentDataPath/house-settings.json，游戏性三开关即改即存，bgm/sfx/窗口模式字段留给 3.6 设置面板）。五页迁移：TitlePage（键盘导航齐全；「继续游戏」禁用占位、「读取存档」进占位页、「新游戏」3.5b 前 Toast 占位）、SavePlaceholderPage（复用 Paper Prefab，§16.5 豁免）、GalleryPage、TitleSettingsPage（存档入口占位）、ExitPage。`OutGameBootstrap` 增 `useHouseUI` 开关（默认关=旧壳基线）。**遗留**：待编辑器确认编译；冒烟=场景里勾上 useHouseUI 走标题层（键盘导航、画廊两 tab、设置三开关落盘重启保留、退出确认、两处存档占位、ESC 返回），再关掉开关确认旧壳无恙；`HouseUI/` .meta 待编辑器生成后补提交。
- **2026-08-09**：3.5a 冒烟通过（修一处 Tween 目标冲突：错峰淡入被 OutGameTweenButton 的按 transform DOKill 误杀，改以 CanvasGroup 为目标 + SetDelay）。**3.5b 完成**——壳扩展：HousePage 增 OnUpdate 每帧钩子（叠加层打开时也调用，时钟走字用）、HouseUIManager 暴露 Canvas；新增 `HouseUIRuntime`（动态表现件最小构建器，注释硬约束：禁止用于布局兜底/列表项）。`OpeningPage`（无 Prefab 纯动画页，开门 1.35s → Hub）；`HubPage` 组装+状态协调（房间切换含卧室开关门过渡、观景收起/展开、家具模式进出禁用 Canvas、访客选择、时钟闸门=进场开退场关）+ 六个 Binder 文件（TopBar 含经济订阅与每帧走字/TaskCard/GuestRail/RightDock 含家具入口/RoomNav/Scene 含烘焙背景+热点+舞台挂接+观景 uvRect）。标题「新游戏」接通：重置各 Manager → 过场 → Hub（无槽位，§16.5）。面板/对话入口 Toast 占位（3.5c）。AnimateHubIn/房间过渡的 Tween 目标均避开按钮 transform（3.5a 教训）。**遗留**：待编辑器确认编译；新壳冒烟=新游戏进 Hub、顶栏走字与时段跨天、房间四切（卧室门过渡）、观景平移缩放与热点跟随、家具模式进出与背景重烘焙、访客到点进场与点击提示、经济条随 GM 面板加钱刷新、ESC 层级（观景→展开、Hub→标题）；OpeningPage/HubPage 等 .meta 待编辑器生成后补提交。
