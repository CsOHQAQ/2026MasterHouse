# 局外系统说明（重写版现状）

> 版本：v2.0（2026-08-10）——局外重构阶段三完成后重写。
> 定位：描述局外系统**当前长什么样、怎么跑起来**，供接手开发（人类或 agent）快速建立地图。
> 架构**决策依据**（为什么这么设计）以 `Docs/架构设计.md` §15/§16 为准，两者冲突时以架构设计为准。
> 重写前的原型代码走读见 `Docs/归档/局外原型代码走读.md`（该批代码已删除，仅存 git 历史）。

## 1. 一句话概括

局外（House 经营）已从「2869 行上帝类 + 代码兜底界面」重写为**按功能模块划分的 MVVM 结构**：内容进 Def 资产、业务逻辑挂全局固定 tick、界面一页一绑定文件且布局全部来自 Prefab。

## 2. 模块地图

| 目录 | 脚本数 | 职责 |
|---|---:|---|
| `Core/` | 3 | `GameManager`（全局固定 tick + 持有各 Manager 与内容表）、`GameConfig`、`OutGameBootstrap`（局外启动入口） |
| `HouseClock/` | 2 | 游戏时钟：`HouseClockData`（Day + 当天 tick 数、时段枚举 `EHousePhase`、时段文案 `HousePhaseText`）、`HouseClockManager` |
| `Economy/` | 4 | 流通数值：`EconomyData` / `EconomyManager` / `EconomyConfig`（SO）/ `HouseGmConsole`（F1 GM 面板） |
| `Visitor/` | 13 | 访客（2026-08-11 重做为「种族模板 → 日程表驱动实例」，规格见 `Docs/待办工作流/访客系统交付说明.md`）：`VisitorRaceDef` / `VisitorScheduleTable` / `NamedVisitorDef`（占位）/ `VisitorTuningConfig`（内容）、`VisitorInstance` / `VisitorData` / `VisitorManager`（业务：五态状态机/两段超时/tag 评分/日结）、`DeterministicRng`（派生种子随机）、表现层四脚本（Stage/Actor/Bubble/SheetAnimator）、示例资产生成器（`Editor/`）。需求 tag 体系 `TagDef` 因两侧共用放 `Core/` |
| `Codex/` | 2 | 图鉴内容 Def：房间/设备/档案/成就/日记 + `CodexTable` |
| `Dialogue/` | 21 | 自研对话系统（2026-08-12 落地，规格见 `Docs/待办工作流/对话系统设计说明.md`）：内容 Def（`DialogueLine` / `DialogueStep` / `DialogueGroupDef` / `DialoguePoolDef` / `DialogueTuningConfig`）、多态事件与条件（`IGameplayAction` / `IGameplayCondition` / `GameplayActions` / `GameplayConditions` / `GameplayContext`）、业务（`DialogueManager` / `DialogueData` / `DialogueRuntime`）、文本（`DialogueTextFormatter` / `INeedPhraseBuilder`）、访客侧接缝（`IDialogueService`）、编辑器（`Editor/` 抽屉+窗口+校验器+示例资产生成器） |
| `HouseUI/` | 77 | 界面壳、页面与叠加层、面板绑定、列表项模板 View（`Views/` 字段袋）、Prefab 生成器（`Editor/`）。含需求交付页（2026-08-12 落地，规格见 `Docs/待办工作流/需求交付页面落地说明.md`）：`DeliveryOverlay` / `DeliveryDragSource`（UGUI 拖拽）/ `Views/DeliveryPageView` / `Views/DeliveryItemView` |
| `Furniture/` | 10 | 家具摆放模式（原型代码保留，已断依赖毒点；内部重构推迟二轮，§16.7） |

命名空间统一 `MasterHouse`（不设子命名空间，§15.1）。

## 3. 运行时结构

### 3.1 启动链路

```
Assets/Scenes/OutGameTest.unity（只含「局外入口」物体）
  └─ OutGameBootstrap.Start()
       ├─ 确保 GameManager 存在（缺则代码创建）
       ├─ HouseUIManager.Build()   → Canvas + EventSystem → TitlePage
       └─ 可选：HouseGmConsole（F1 面板，Inspector 勾选项）
```

与局内 `TestScene` 互不加载、互不干扰（§16.10）；局内外正式联通待定 #19。

### 3.2 tick 驱动（§16.4）

局内局外**共用同一心跳**。`GameManager.Update()` 是整个逻辑层唯一接触真实时间的位置，把流逝时间折算成固定步长：

```
GameManager.RunTick()
  ├─ LevelManager.TickAll()      局内节点模拟（打烊后停 tick，§16.4 打烊闸门）
  ├─ HouseClockManager.Tick()    时钟推进（闸门开时；到打烊时刻停走）
  └─ VisitorManager.Tick()       访客日程投放/状态机推进/超时（打烊时业务 tick 整体停表）
```

- 体验节奏「现实 1 秒 = 游戏 1 分钟」以 tick 表达：`GameConfig.HouseTicksPerGameMinute`（默认 10，待定 #18）。
- **时钟闸门是原因集合（2026-08-12）**：`HouseClockManager.SetStopReason(EClockStopReason, bool)`，任一原因存在即关闸。持有者两个——`OffHubPage`（时间只在 Hub 页面流动，家具模式不退页所以时钟照走）与 `ModalDialogue`（模态对话框期间，对话设计说明 §8）。派生出两个量，**不要混用**：
  - `IsRunning`（时钟推进 + `VisitorManager.Tick`）= 无任何停走原因且未打烊
  - `IsWorldFrozen`（门控 `LevelManager.TickAll`）= 打烊 **或** 模态对话框，**不含**页面闸门——局内测试场景没有 HubPage，跟着 `IsRunning` 走产线会永不推进
- **打烊闸门（2026-08-11）**：到达打烊时刻（`VisitorTuningConfig.closeMinute`）后一切 tick 业务冻结；玩家清完场上访客后点【结束今天】→ 日结面板 → 跳次日开门时刻。打烊是当天时间的纯函数（`IsClosedForToday`），不进原因集合。
- 所有时段划分、到访判定一律**整数比较**（分钟/tick），禁止 float 小时累加。

### 3.3 数据流

```
Def 资产（Model，运行时只读）
   ↓ GameManager 统一加载
Manager（业务逻辑，唯一修改入口）
   ↓ 事件广播（离散变化）/ 每帧轮询（连续量）
HouseUI 页面与绑定（View，只读）
```

表现层**不回写业务**：访客舞台只读 `VisitorManager` 的状态生成演员，到访与否由业务层在 tick 内判定。

## 4. 数据资产（`Assets/Resources/OutGameUI/`）

| 资产 | 内容 |
|---|---|
| `VisitorScheduleTable.asset` | 访客日程：第几天/出现时刻/种族（零随机零上限；缺失可经菜单 MasterHouse → 访客系统 → 创建示例资产补齐） |
| `VisitorTuningConfig.asset` | 营业时段（开门/打烊时刻）、闲逛冒泡节奏、氛围邻居名册 6 |
| `VisitorRaces/Race_*.asset` | 种族模板 4（性格数值/需求权重/立绘差分/序列帧路径 + `dialoguePool` 引用），沿用原 4 个动物访客素材 |
| `DialogueTuningConfig.asset` | 对话调参：打字机速度、recent 去重环长度 N |
| `Assets/GameData/Dialogue/Pool_*.asset` | 种族对话池 4（八个触发分类 + 四档交付预览单句）；被种族引用而非按路径加载，故不在 Resources 下 |
| `Assets/GameData/Dialogue/通用/Group_*.asset` | 对话组（一组一资产，四个种族共用示例内容）。缺失可经菜单 MasterHouse → 对话系统 → 创建示例资产补齐 |
| `Assets/GameData/Tags/Tag_*.asset` | 需求/物品标签森林（局内局外共用，故不在 OutGameUI 下） |
| `CodexTable.asset` | 房间 4 / 设备 9 / 档案 9（两分类）/ 成就 4 / 日记 2 |
| `FurnitureTable.asset` | 家具 13（占格/尺寸/价格/解禁声望/装饰分/桌面格配置） |
| `FurnitureRoomTable.asset` | 房间 1（起居室）：三层背景、网格、场景占用格、初始摆放 |
| `HouseEconomyConfig.asset` | 流通数值参数（初始值/服务奖励/惩罚/装饰分权重） |
| `Prefabs/` | 44 个界面 Prefab（交付页 `DeliveryPage` 与仓库条目模板 `DeliveryItemRow` 由生成器在下次进入编辑器时补齐） |

**改内容 = 改资产，不碰代码**；加内容 = 加行，不改代码（§16.6）。设置项（音量/窗口模式/游戏性开关）不属于进度数据，独立存于 `persistentDataPath/house-settings.json`（§16.5）。

## 5. 界面体系（HouseUI）

### 5.1 壳与页面

`HouseUIManager` 负责 Canvas 生命周期、**整页路由**与**叠加层栈**。ESC 语义统一为「先弹栈、再问当前页」：

```
页面（HousePage 派生，一页一文件）
  TitlePage / SavePlaceholderPage / GalleryPage / TitleSettingsPage / ExitPage / OpeningPage / HubPage

叠加层（IHouseOverlay，可退栈）
  PanelHost（9 类系统面板）/ DialogueOverlay（GVN 对话层）/ SettingsOverlay（Hub 设置）
```

**对话层的开合由业务事件驱动，不由玩家点击驱动**（2026-08-12）：`HubPage` 订阅
`DialogueManager.PlaybackStarted / PlaybackEnded` 来开关 `DialogueOverlay`。玩家点前台访客只是发起一次
`VisitorManager.RequestFirstMeeting`；接待/拒绝是【初次见面】对话末尾**分支选项上的事件**，
不再有 UI 硬编码按钮。ESC 退栈 → `DialogueManager.Interrupt()`（中断语义见对话设计说明 §5.2）。

`HubPage` 只做组装与状态协调，各区块绑定拆在六个 Binder 文件（TopBar / TaskCard / GuestRail / RightDock / RoomNav / Scene）。

### 5.2 硬约定（§16.2）

- **Prefab 是布局唯一真相源**：页面/面板只从 Prefab 实例化并绑定数据，**缺失打 Error 不回退代码布局**。同一界面禁止 Bind/Build 双实现。
- **动态列表项 = 模板 Prefab + 运行时实例化**：设备卡、档案卡、日记文章、成就行、商城卡各有模板。
- **运行时构建仅限动态表现件**：`HouseUIRuntime` 服务 Toast、开门过场、房间切换门扇、家具热点、经济数值条这类临时视觉件；它**不得**用于页面布局或列表项。（例外记录：原型期未 Prefab 化的家具 HUD 与 GM 面板亦由其服务，二轮收紧。）

### 5.3 Prefab 生成器

`HouseUI/Editor/OutGameUIPrefabGenerator`：编辑器聚焦时**只补缺失、不覆盖手调**；`Tools/MasterHouse/OutGame UI/Rebuild Default Prefabs...` 才会带确认弹窗覆盖重建。新增界面时先在生成器里写默认布局，生成后在编辑器手调。

序列化约束（RETRO 教训）：每个挂 Prefab 的 MonoBehaviour 必须独占同名 .cs 文件。

## 6. 明示豁免与已知遗留

**功能豁免**（架构定稿时明确放弃「与原型等价」的部分）：

| 项 | 说明 | 依据 |
|---|---|---|
| 存档功能移除 | 标题「继续游戏」禁用、「读取存档」为占位页；旧 PlayerPrefs 存档已退役 | §16.5，待定 #9 |
| 仓库/个人/通讯录 | 统一「尚未开放」占位页 | §16.8 |
| Hub 设置面板 | 复用标题设置 Prefab，旧版音量滑条/窗口模式未迁移 | §16.8 |

**代码层遗留**（推迟到二轮或等需求）：

- 家具 `FurnitureRoomController`（874 行）内部未 MVVM 化、锚点数学在 Controller 与 Composer 双实现、HUD 未 Prefab 化（§16.7 推迟二轮）；家具单房间硬编码 `rooms[0]`。
- 存档接缝占位：`EconomySaveData`、`EconomyManager.Capture/Restore`、`VisitorManager.Capture/Restore`（需求不入档、按派生种子重算）当前**无调用方**，保留待统一存档接入（待定 #9）。
- ~~`DialogueOverlay` 是访客状态机的临时 debug 驱动层~~ **已删除（2026-08-12）**：接待/拒绝改由对话分支选项的事件驱动。~~物品提交没有去处~~ **已就位（2026-08-12）**：交付页落地，Hub 点「服务中」的访客即开页；GM 面板那颗临时的「给服务中的访客递上仓库首项」按钮已随之删除。
- ~~交付预览（`DialogueManager.PreviewLine`）无调用方~~ **已接通（2026-08-12）**：交付页调用，档位由 `VisitorManager.Preview` 试算（与 `Submit` 共用同一份 `Evaluate`）。
- `ItemDef.icon` 字段已建但**全部为空**（美术未接入）：交付页仓库条目按 `DisplayColor` 画占位色块，缺图不报错。
- 日历面板读 `DateTime.Now` 渲染现实日历，与游戏 Day 体系脱节。
- 原型假数据：委托面板三条任务、成就「前两项已完成」、任务卡进度百分比。
- 访客门口/游走坐标硬编码（仅覆盖起居室）；动物序列帧为占位素材。
- 对话内的「家具快捷栏」与真实家具布局无数据联系（纯表现）。

## 7. 相关文档

- `Docs/架构设计.md` — 目标架构与决策记录（权威），§15 目录规则、§16 局外架构
- `Docs/四宫格房间与访客拖拽说明.md` — Hub 场景 2×2 连续世界 + 自由相机 + 访客拖拽换房（2026-08-13）
- `Docs/待办工作流/局外重构工作流.md` — 重构执行清单与逐步进度日志
- `Docs/PREFAB_UI_GUIDE.md` — Prefab 编辑约定
- `Docs/归档/局外原型代码走读.md` — 重写前的原型现状（已归档）
- `Docs/归档/RETROSPECTIVE.md` — 工程踩坑库（Unity 序列化/DOTween/伪空值等，仍有参考价值）
