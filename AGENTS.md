# MasterHouse Agent 入口

## 协作重心

- 开始处理本项目任务前，先读取 `X:\Lilith\House\DesignDoc\00-MasterHouse项目上下文.md`。
- 用户是项目策划；以同组策划/开发搭档的方式协作，主动指出体验风险、规则冲突、实现成本与范围问题。
- 本项目对话以**游戏策划、游戏设计和玩家体验**为首要目标。讨论顺序优先是：设计目的与玩家体验 → 核心规则和内容节奏 → Demo 范围 → 技术实现与成本。
- 技术架构用于判断可行性、拆分落地路径和暴露风险，不得仅因当前代码结构或原型限制就替代设计判断、倒推最终玩法。
- 在有限开发周期内，应给出兼顾设计意图与交付风险的 MVP 方案；如理想设计与当前产能冲突，分别说明目标形态、Demo 裁剪和后续扩展，不默默删改设计目标。

## 文档权威层级

1. 游戏设计以 `X:\Lilith\House\DesignDoc` 下与当前议题直接相关的文档为最新来源。
2. `X:\Lilith\House\Doc_Intro` 是原始立项材料，用于理解产品愿景与历史方案；与 `DesignDoc` 冲突时，不默认覆盖较新的设计文档。
3. `X:\Lilith\House\TechDoc` 保存跨对话使用的工程与项目技术总结，包括架构设计、局外系统现状和重构工作流。
4. 修改工程时，同时读取仓库内 `X:\Lilith\House\2026MasterHouse\Docs` 的对应版本；它与代码共同构成当前实现上下文。`TechDoc` 与仓库内副本不一致时，先比较版本与变更内容并向用户说明，不擅自选择一份覆盖另一份。
5. 代码和配置资产代表当前已实现状态，不自动等同于最终策划定案；游戏设计冲突由策划目标决定，工程差异则作为实现风险记录。
6. 文档中的“预计”“待定”“需要设计”、空章节和示例内容均视为草案；发现文档互相矛盾时，应明确指出并向用户确认，不自行替项目拍板。

## 按任务读取

- 讨论局内节点、管线或关卡：读取 `DesignDoc\InGame\局内(连连看).md`。
- 讨论 House、Item、经济或商店：读取 `DesignDoc\System\House家园玩法\大House.md` 与 `DesignDoc\System\局外(汇总)\局外(汇总).md`。
- 讨论访客、需求、对话或通讯录：读取 `DesignDoc\System\访客功能\访客功能.md` 与局外汇总中的访客章节。
- 纯策划讨论默认先从设计目标和玩家体验出发；只有当实现成本、现有能力或排期会实质影响方案时，再读取对应技术资料进行约束分析。
- 修改代码前：完整读取 `TechDoc\架构设计.md` 和仓库内 `Docs\架构设计.md`，尤其是 §11、§13、§15、§16；局外工作还要读取当前重构工作流。待定项保留占位并引用编号，不擅自确定。
- 判断当前已实现功能或重构进度：读取 `TechDoc\局外系统说明.md`、`TechDoc\待办工作流\局外重构工作流.md`，并以代码、配置资产、Git 状态交叉验证。

## 文档归档

- 所有游戏设计相关的新建、整理和评审文档统一放在 `X:\Lilith\House\DesignDoc`，不要放入 Unity 工程目录。
- 跨对话共享的工程总结与技术方案放在 `X:\Lilith\House\TechDoc`；必须随代码版本管理的架构约束、测试说明和开发工作流保留在 `X:\Lilith\House\2026MasterHouse\Docs`。需要双份维护时应明确同步，避免静默漂移。
- 经过团队或用户确认的重要结论应更新进对应设计文档或项目上下文；探索性想法不得直接写成既定规则。
- 本 `AGENTS.md` 仅作为跨对话自动发现的入口与资料路由，不承载设计正文。
