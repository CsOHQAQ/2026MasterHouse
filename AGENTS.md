# MasterHouse Agent 自动发现入口

- 开始处理本项目的任何任务前，必须完整读取 `X:\Lilith\House\TechDoc\AGENTS.md`，并将其视为本工程的主协作规范。
- `X:\Lilith\House\TechDoc\AGENTS.md` 负责文档权威层级、按任务读取规则、设计协作原则与归档规则；不得只读取本入口后直接开始工作。
- 本文件必须保留在 Unity 工程根目录，用于工作区自动发现。除非 Codex 的指令发现范围发生变化，否则不要将本入口一并移出工程。
