# CLAUDE.md

本项目"MasterHouse"（仓库名 2026MasterHouse）正在进行运行架构重构：局内（节点玩法）逻辑层已按新框架实现，局外（House 经营）为合并进来的原型代码、处于休眠待重构状态（现状见 `Docs/局外系统说明.md`）。

- **架构权威参考：`Docs/架构设计.md`**。动手写任何代码前必读，特别是 §11【确定性与存档守则】和 §13【待定问题清单】——遇到待定项留占位符并注释编号，不要自行拍板。局外相关工作另需读 §16【局外系统架构】。
- Unity 2022.3，URP（2D）。代码注释、UI 文案、提交信息使用**简体中文**。
- 命名空间统一为 `MasterHouse`（不设子命名空间）。目录按**功能模块**划分：Core / 局内模块 / 局外各功能模块，规则与目标结构见设计文档 §15。
- 架构为 MVVM：Model（策划配置的 Def，运行时只读）→ ViewModel（数据类 + Manager）→ View（只读表现层）。逻辑层以固定 tick 推进，禁止 `Time.deltaTime`。
- 局外 UI 硬约定：**Prefab 是布局唯一真相源**，禁止 Bind/Build 双实现与代码兜底布局（§16.2）；存档一律 JSON 文件，禁止 `PlayerPrefs`（待定 #9）。
